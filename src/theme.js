import { blue } from "@mui/material/colors";
import { createTheme } from "@mui/system";

export const theme = createTheme = createTheme({
    palette:{
        primary:{
            main: blue[500],
        }
    }
})